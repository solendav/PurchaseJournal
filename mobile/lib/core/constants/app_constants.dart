class AppConstants {
  AppConstants._();

  static const appName = 'Purchase Journal';
  static const appTagline = 'Track purchases, suppliers, and debt in one place';
  static const connectionTimeout = 30000;
  static const receiveTimeout = 30000;
}
