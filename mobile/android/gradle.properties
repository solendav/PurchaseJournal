org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
android.builtInKotlin=false
android.newDsl=false
# Flutter 3.44 enables R8 minification by default (shrink=true). ML Kit text recognition
# references optional language modules we don't bundle — disable shrink for release builds.
shrink=false
